# .env.example

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=username
DB_PASSWORD=password
DB_NAME=database_name

# JWT settings
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=1h

# Server port
PORT=3000
